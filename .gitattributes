# Large data files - use LFS for test datasets
10M.csv filter=lfs diff=lfs merge=lfs -text
2M_test.csv filter=lfs diff=lfs merge=lfs -text

# Tables folder - track small CSV files
tables/*.csv !filter !lfs
